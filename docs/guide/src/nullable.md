# Nullable

JVerify allows detecting all possible `NullReferenceException`s in your Java code. If a reference variable can contain the `null` value, then it must be annotated with `@Nullable`.

For example:
```java


```



